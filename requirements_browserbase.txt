# Core dependencies
pyyaml>=6.0
openpyxl>=3.0.10
pandas>=1.5.0
numpy>=1.22.0
python-dateutil>=2.8.2
nltk>=3.7
spacy>=3.4.0
tqdm>=4.64.0

# Logging and monitoring
colorlog>=6.7.0

# Data visualization (for potential dashboard extension)
matplotlib>=3.6.0
seaborn>=0.12.0

# Test dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Browserbase API dependencies - not required for local installations
# as these are handled by the cloud service
# The antml package would typically be installed in a Claude environment
# In a real-world scenario, you would use the official Browserbase SDK
